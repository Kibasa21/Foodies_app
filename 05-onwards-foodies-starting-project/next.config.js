/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    ...nextConfig,
    images: {
      domains: ['kaotxhjorcwnojdtcsrl.supabase.co', 'example.com', 'another-domain.com'],
    },
  };
